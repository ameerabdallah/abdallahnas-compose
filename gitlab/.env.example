# GitLab Configuration

# Container User/Group IDs
PUID=1000
PGID=1000

# Timezone
TZ=America/New_York

# Storage Paths
DOCKER_PATH=/mnt/path/to/docker
FLASHY_PATH=/mnt/path/to/flashy

# GitLab External URL (change to your domain or IP)
GITLAB_EXTERNAL_URL=http://gitlab.yourdomain.com

# GitLab Root Password (change this!)
GITLAB_ROOT_PASSWORD=changeme_please

# GitLab Runner Registration Token (will be generated after first GitLab startup)
# Leave empty initially, then get from GitLab UI: Admin Area > CI/CD > Runners
GITLAB_RUNNER_TOKEN=

# PostgreSQL Configuration (optional - GitLab uses internal PostgreSQL by default)
# Uncomment and configure if you want to use your existing PostgreSQL instance
# PG_HOST=postgres
# PG_PORT=5432
# PG_USER=gitlab
# PG_PASS=gitlab_password
# PG_DATABASE=gitlab_production

# SSH Port (if you want to use a different port than 22)
GITLAB_SSH_PORT=2222
